<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#E5EDF1"
    tools:context=".ui.TKB.fragment2">
    <com.harrywhewell.scrolldatepicker.DayScrollDatePicker
        android:id="@+id/day_date_picker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        app:baseColor="@color/tertiary"
        app:baseTextColor="@color/primary"
        app:selectedColor="@android:color/holo_red_light"
        app:selectedTextColor="@color/primary"
        app:showTitle="false"

        />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/frag2_rcv"
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="10dp"
        android:paddingRight="10dp"
        android:layout_margin="10dp"
        android:visibility="gone"/>

    <ProgressBar
        android:id="@+id/tkb_fragment2_loading"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:visibility="gone"
        android:layout_height="match_parent"
        android:layout_gravity="center|center_horizontal"/>

</LinearLayout>